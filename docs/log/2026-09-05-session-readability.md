# Session readability

Saved and pending sidebar rows share a two-line, stronger title treatment and
readable metadata. Full titles remain in tooltips; session order, PR badges,
provider routing and personalization are unchanged.

Both composers keep flexible, bounded model/provider labels with full tooltips
and picker details, 32px action targets and stronger placeholder ink. Changes labels, paths,
counts and actions use readable existing type steps. Pane-header actions wrap as
one group at narrow widths rather than cutting off the Files/Changes switcher.
No palette, zoom/default
font preference, process behavior or change-inventory logic was changed.

Electron coverage exercises long session/model labels in both themes, laptop
widths and increased zoom, checking control containment and capturing each state.
